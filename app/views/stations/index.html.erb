<h1>Listing stations</h1>

<table>
  <tr>
    <th>Url</th>
    <th>Name</th>
  </tr>

<% @stations.each do |station| %>
  <tr>
    <td><%= station.url %></td>
    <td><%= station.name %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Station', new_station_path %>
